import { LoadingOverlay } from "@/components/VortexLoader";

export default function RHProLoading() {
  return <LoadingOverlay message="Cargando RH Pro..." hint="Trayendo contratos y asignaciones" />;
}
