import { LoadingOverlay } from "@/components/VortexLoader";

export default function EventualesLoading() {
  return <LoadingOverlay message="Cargando eventuales..." hint="Construyendo calendario" />;
}
